const form=document.getElementById("checkForm"),u=document.getElementById("username"),h=document.getElementById("handle"),r=document.getElementById("results"),m=document.getElementById("message");form.addEventListener("submit",async e=>{e.preventDefault();const name=u.value.trim().replace(/^@/,"");if(!name){m.textContent="Please enter a username.";return}m.textContent="Starting demo analysis…";try{const res=await fetch("/api/demo-check",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:name})});const data=await res.json();if(!res.ok)throw new Error(data.error||"Request failed");h.textContent="@"+data.username;r.hidden=false;m.textContent=data.notificationSent?"Demo analysis ready. Notification sent.":"Demo analysis ready. Telegram notification is not configured.";r.scrollIntoView({behavior:"smooth",block:"nearest"});}catch(err){m.textContent="Unable to start the demo analysis: "+err.message;}});