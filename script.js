const form=document.getElementById("checkForm"),u=document.getElementById("username"),h=document.getElementById("handle"),r=document.getElementById("results"),m=document.getElementById("message");form.addEventListener("submit",e=>{e.preventDefault();const name=u.value.trim().replace(/^@/,"");if(!name)return;h.textContent="@"+name;r.hidden=false;m.textContent="Analysis preview ready. Real metrics require authorized data access.";r.scrollIntoView({behavior:"smooth",block:"nearest"});});