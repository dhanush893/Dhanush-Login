const form=document.getElementById("campaignForm"),u=document.getElementById("username"),goal=document.getElementById("goal"),m=document.getElementById("message");form.addEventListener("submit",e=>{e.preventDefault();const handle=u.value.trim().replace(/^@/,"");if(!handle){m.textContent="Enter a public username.";return}m.textContent=`Campaign preview created for @${handle}: ${goal.value}.`;});