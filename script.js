const form=document.getElementById("campaignForm"),username=document.getElementById("username"),preview=document.getElementById("previewName"),message=document.getElementById("message");let goal="Profile discovery";document.querySelectorAll(".goal").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll(".goal").forEach(x=>x.classList.remove("active"));btn.classList.add("active");goal=btn.dataset.goal}));username.addEventListener("input",()=>{preview.textContent=username.value.trim().replace(/^@/,"")||"yourcreator"});form.addEventListener("submit",e=>{e.preventDefault();const handle=username.value.trim().replace(/^@/,"");if(!handle)return;message.textContent=`Campaign preview ready for @${handle} — focus: ${goal}.`;});